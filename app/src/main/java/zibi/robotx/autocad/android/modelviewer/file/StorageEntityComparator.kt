package zibi.robotx.autocad.android.modelviewer.file

class StorageEntityComparator {//}: Comparator<StorageEntity> {

//    override fun compare(lhs: StorageEntity, rhs: StorageEntity): Int {
//        if (lhs is StorageFolder && rhs is StorageFile) {
//            return -1
//        }
//        return if (lhs !is StorageFile || rhs !is StorageFolder) {
//            lhs.name.compareTo(rhs.name)
//        } else 1
//    }
}